# كينو — تحديث واجهة المتجر والحركة

نسخة مبنية على مستودع `mostafak80/keno-store`، مع الحفاظ على الكتالوج والأسعار وبيانات التحويل وملفات الحسابات والطلبات الحالية.

## تشغيل النسخة

المشروع HTML/CSS/JavaScript، ولا يحتاج إلى npm أو عملية build. للتجربة المحلية شغّل من مجلد الموقع:

```sh
python3 -m http.server 8000
```

ثم افتح `http://localhost:8000`. إدارة الموقع الحالية من `#admin` أو الاختصار `Ctrl+Shift+A`. تسجيل الدخول والنشر السحابي يستخدمان إعدادات المشروع الحالية؛ لم تُضف بيانات دخول جديدة.

ارفع محتويات المجلد إلى جذر مشروعك الحالي مع الحفاظ على إعدادات Vercel وFirebase ومتغيرات البيئة الموجودة لديك. النشر من هذه النسخة لم يُنفّذ عن بُعد؛ اتصال GitHub المتاح أثناء العمل يسمح بالقراءة فقط. احتفظ بنسخة احتياطية قبل استبدال الملفات، خصوصًا لو عدّلت الكتالوج بعد هذه النسخة.

## أين تعدّل؟

| المطلوب | الملف / المكان |
| --- | --- |
| ألوان الواجهة والمسافات والموبايل والحركات | `assets/theme.css` لألوان الوضعين؛ و`assets/storefront.css` لتنسيق المتجر |
| عنوان الرئيسية والنصوص وترتيب الأقسام | `index.html` |
| طريقة اختيار مختارات كينو والأقسام ونسخ الدفع | `js/storefront.js` |
| الأسعار والخدمات وطرق الدفع | لوحة الإدارة الحالية أو `assets/catalog.js` |
| عرض الرئيسية، الربط بالبحث والسلة | `app.js` |
| إعداد ومعاينة العرض الرئيسي في الإدارة | `js/admin.js` |
| باقي تنسيقات الإدارة والطلب | `style.css` |

ملفا الواجهة الجديدان منفصلان عن ملفات الإدارة، ويُحمّلان بعد التنسيقات الأساسية وقبل التطبيق حسب ترتيب `index.html`. أبقِ هذا الترتيب.

## سلوك الواجهة

- البحث العلوي والبحث في الكتالوج يستخدمان محرك البحث الحالي نفسه، بالعربية والإنجليزية. الضغط على قسم يمسح البحث السابق ويعرض خدمات هذا القسم.
- أعداد الأقسام مأخوذة من الخدمات الظاهرة فقط. الأقسام الفارغة لا تظهر في مربعات الاستكشاف.
- مختارات كينو تعرض حتى أربع خدمات متنوعة لها باقات متاحة. الأولوية للخدمات التي عليها علامة «مميزة»، مع تنويع الأقسام. كل بطاقة تعرض أرخص باقة متاحة باسمها وسعرها، وتفتح العرض نفسه مباشرة.
- سعر ومسمى العرض الرئيسي يُقرآن من العرض المرتبط؛ حقلا السعر والمسمى في إعداد العرض للقراءة فقط. لتغييرهما، عدّل العرض من الخدمات. العرض يختفي عند إخفاء الخدمة أو تعطيل العرض.
- فودافون كاش وإنستاباي وتلدا تستخدم بيانات التحويل الحالية دون تغيير. قسم الدفع الجديد يتبع طرق الدفع المفعّلة في الإدارة، ونسخ البيانات لا ينفّذ تحويلًا ولا يؤكد سدادًا.
- الحركة تشمل العرض الرئيسي، انعكاس الإضاءة، ظهور البطاقات عند التمرير، وانتقالات الأزرار. تم حذف أزرار إيقاف الحركة. المؤثرات قصيرة وغير مستمرة؛ إعداد تقليل الحركة في الجهاز له الأولوية. تتوقف الحركة عند إخفاء تبويب المتصفح.
- ظهور الأقسام لا يعتمد على الحركة: تظل المحتويات ظاهرة إذا لم يتوفر مراقب التمرير.
- صور الخدمات الحالية مدعومة، مع بديل نصي وأيقونة في بطاقات المختارات عند تعذّر تحميل الصورة. لم تُنسخ صور أو شعارات من الموقع المرجعي.

## التحقق

```sh
node --test tests/*.test.cjs tests/*.test.mjs
node validate_all.js
```

25 اختبارًا للكتالوج وثبات الأسعار والدفع والبحث ورسائل الطلب، والتحقق من رموز تسجيل الدخول ومنع نشر ملف غير صالح أو قديم. اختبار المتصفح:

```sh
npm install --no-save playwright
npx playwright install chromium
node tests/browser.cjs
```

يمكن تحديد مسار متصفح موجود بمتغير `CHROMIUM_EXECUTABLE_PATH`، وحفظ صور الفحص بمتغير `QA_SCREENSHOTS`. اختبار المتصفح يمنع جميع الاتصالات الخارجية ويستخدم بدائل محلية للطلب والنشر؛ لا يرسل طلبات حقيقية.

تفاصيل الإصلاحات وحدود التحقق وخطوات الرفع في `BUGFIX-REPORT.md`.

## كروت الخدمات والعروض المصغرة — 22 سبتمبر 2026

التنسيقات في `assets/compact-cards.css`، وتُحمّل بعد `assets/theme.css`.

- الكمبيوتر يعرض 4 كروت خدمات في الصف، ثم 3 أو 2 حسب المساحة. الموبايل يعرض كارتين في الصف، وصورة/أيقونة صغيرة واسم الخدمة والسعر وزر الأسعار. الوصف والتصنيف والتوفير التفصيلي متاحون داخل الخدمة بدل تكرارهم في الكارت الصغير.
- اسم الخدمة على الموبايل 16px والسعر 22px. ارتفاع كروت الصف الأول نحو 177px في المقاسات المختبرة؛ الأسماء الطويلة قد تزيد الارتفاع للحفاظ على النص.
- عروض الخدمة شبكة من 3 أعمدة، وعمودين تحت 380px. أسماء العروض كاملة والأسعار بارزة، مع تمييز العرض المختار. لا تُحذف عروض أو شروط من البيانات.
- صورة نافذة الخدمة صارت صغيرة. أزرار العروض / بيانات الطلب / الدفع في أعلى النافذة تنقل العميل مباشرة للقسم المطلوب، مع تثبيت إجراءات الطلب أسفل النافذة وتمرير المحتوى الأوسط فقط.
- منطق الانتقال في `app.js` يستهدف حاوية النافذة نفسها حتى لا تتحرك الصفحة الخارجية أو تختفي أزرارها.

فحص Chromium محلي في الوضعين على عروض 320 و390 و430 و768 بكسل: كارتان للخدمات، عمودان أو ثلاثة للعروض، عدم وجود تمدد أفقي، اختيار عرض يحدّث ملخص الطلب، وعمل أزرار الانتقال الثلاثة. فُحص فتح 5 خدمات إضافية على 320px. الأسعار والكتالوج وطرق الدفع لم تتغير. التعديلات غير مرفوعة للموقع الحي.

## ضبط كثافة شاشة الطلب والوضع الافتراضي

- `assets/compact-cards.css`: مقدمة الخدمة 40px للصورة، والارتفاع حسب النص؛ حقول الحساب 40px بخط 16px، وطرق الدفع ثلاثة كروت مغلقة. لا تضف ارتفاعًا ثابتًا لحاوية النصوص، ولا تسمح بانكماش عناصر منطقة التمرير حتى لا تتداخل.
- `app.js`: الدالة `renderPaymentMethodsList` تعرض تفاصيل الدفع مغلقة عند الفتح؛ اختيار الوسيلة منفصل عن عرض تفاصيلها. الحدث يفتح وسيلة واحدة ويحدّث `aria-expanded` و`aria-pressed`.
- `js/theme.js`: الاختيار المحفوظ إن كان صالحًا، وإلا `dark`. إعداد ألوان نظام التشغيل لا يغيّر البداية. التبديل اليدوي والتزامن بين التبويبات مستمران.
- `assets/theme.css`: التصحيحات الأخيرة محددة بـ `html[data-theme="light"]` للنصوص والأيقونات الضعيفة في الوضع الفاتح.
- روابط الملفات المعدّلة في `index.html` تحمل رقم إصدار؛ حدّثه عند تعديل هذه الملفات لاحقًا لتفادي نسخة المتصفح القديمة.

ارفع جميع محتويات الحزمة مع الحفاظ على المجلدات. لم تُغيّر بيانات الأسعار أو وسائل التحويل. هذه النسخة اختُبرت محليًا ولم تُرفع تلقائيًا إلى GitHub.
