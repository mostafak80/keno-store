# إصلاحات كينو ستور — 22 سبتمبر 2026

النسخة مبنية على مستودع `mostafak80/keno-store`، commit `c90c3cb2fac1630312b166ab44c8c7e4ceb83d22`.

## ما تغيّر

- توحيد ألوان الوضعين في `assets/theme.css` وإزالة تنسيقات متضاربة كانت تخفي الكتابة.
- زر واحد للوضع الفاتح/المظلم؛ الاختيار محفوظ، ويُطبّق قبل عرض الصفحة. عند عدم وجود اختيار يبدأ بالمظلم، حتى لو إعداد الجهاز فاتح. يعمل الزر حتى عند حظر التخزين المحلي.
- تحسين تباين الآراء، البطاقات، الأسعار، شاشة الطلب، تفاصيل الدفع ولوحة الإدارة.
- حذف أزرار إيقاف الحركة؛ مؤثرات ظهور وانتقالات قصيرة، مع احترام تقليل الحركة في الجهاز وإيقاف الحركة عند إخفاء التبويب.
- إصلاح متغير إيصال غير معرّف كان يوقف تهيئة الموقع عند قراءة السلة الفارغة.
- تنظيف عناصر السلة القديمة/غير المتاحة قبل إجراء عمليات الحذف والكمية، وضبط الكمية بين 1 و99.
- دعم Escape والتنقل بلوحة المفاتيح في السلة، مع استعادة التركيز ومنع التفاعل مع الخلفية.
- إصلاح اختيار طرق الدفع بالراديو ولوحة المفاتيح؛ تفاصيل وسيلة واحدة فقط تظهر في كل مرة.
- عدم إعلان نجاح النشر إذا فشلت جميع الوجهات. تبقى المسودة قابلة لإعادة المحاولة، مع توضيح النشر الجزئي.
- تحقق الخادم من توقيع Firebase ID token والمشروع والصلاحية والبريد الموثق، والتحقق من محتوى الكتالوج قبل إرساله إلى GitHub. رفض قاعدة قديمة لتفادي الكتابة فوق تعديل متزامن في مسار GitHub.
- إصلاح مؤشر المبيعات الذي كان يعرض `NaN` بسبب قراءة اسم حقل خاطئ.
- فتح تفاصيل الخدمة من أعلى الصفحة بدل الاحتفاظ بموضع تمرير الخدمة السابقة.
- أرقام طلبات أطول تشمل الوقت وقيمة عشوائية لتقليل التصادمات.
- إصلاح انتهاء جلسة الإدارة بسبب الخمول: قراءة مؤقت الجلسة لم تعد تجدّدها كل ثانية.
- انتظار اشتراك الطلبات غير المتزامن، وتوحيد أسماء أدوار Firestore، وتقييد قراءة قائمة الإدارة.
- تحويل مسار `/admin` على Vercel إلى `/#admin` لتجنب أخطاء المسارات النسبية.

ملف `assets/catalog.js` لم يُعدّل؛ الأسعار والخدمات وبيانات فودافون كاش وإنستاباي وتلدا كما كانت في النسخة المرجعية.

## حدود التحقق والتشغيل

الاختبارات محلية ومعزولة عن الخدمات الخارجية؛ لا يوجد تحويل مالي أو رسالة واتساب مرسلة أو بيانات إنتاج مكتوبة. تسجيل Google الحقيقي، قواعد Firestore بعد النشر، وإعدادات استضافة Vercel تحتاج فحصًا بحساب المالك بعد رفع النسخة. قواعد Firestore مرفقة لكنها لم تُنشر ولم تُختبر بمحاكي Firebase.

GitHub Pages يشغّل الواجهة الثابتة فقط؛ `/api/publish` يحتاج Vercel/خادم Node. احتفظ بإعدادات Firebase الحالية. متغيرات الخادم: `GITHUB_TOKEN` و`GITHUB_REPO` و`ADMIN_EMAILS`؛ اختياريًا `GITHUB_BRANCH` و`FIREBASE_PROJECT_ID`. لا تضع التوكن داخل HTML أو JavaScript المرسل للعميل.

مسار النشر إلى Firebase ما زال يعتمد على إعداد المشروع وقواعده؛ فحص التعارض المضاف خاص بمسار GitHub. هذه مراجعة وإصلاحات للمشكلات المكتشفة وليست ضمانًا بعدم وجود أي خطأ أو شهادة تدقيق أمني شامل.

## رفع النسخة

فك الضغط وضع محتويات الحزمة في جذر المستودع مع الحفاظ على بنية المجلدات. قارن أي تعديلات أحدث من النسخة المرجعية قبل استبدالها. ارفع الملفات الجديدة خصوصًا `assets/theme.css` و`server/firebase-token.mjs`. افتح `/#admin` للوصول إلى الإدارة. طبّق `firestore.rules` من إعدادات مشروع Firebase عند اعتماد قواعد الوصول الجديدة.

صلاحية GitHub المتاحة أثناء العمل كانت قراءة فقط؛ لم تُرفع التعديلات إلى المستودع أو الموقع الحي.

## نتائج التحقق المحلي

- نجاح 25 اختبارًا آليًا للكتالوج والبحث والطلب والتحقق من هوية الناشر ومحتوى النشر وتعارض نسخة GitHub.
- اختبار Chromium للفاتح والمظلم، استمرار الاختيار بعد إعادة التحميل، البحث العربي والإنجليزي، طرق الدفع الثلاثة، تجهيز رسالة الطلب، إضافة السلة وتعديل الكمية والحذف مع عناصر قديمة، وإغلاقها بـ Escape.
- عدم وجود تمدد أفقي للمتجر عند عروض 320 و390 و768 و1440 بكسل.
- فحص واجهة الإدارة ببيانات محلية بديلة، والتحقق أن فشل النشر يبقي المسودة والتعديلات؛ لا يمثل تسجيل دخول سحابيًا حقيقيًا.
- فحص تقليل الحركة وحظر التخزين المحلي، وصياغة ملفات JavaScript/CSS وتفرد معرّفات HTML ووجود الموارد المحلية.
- لم تُرصد استثناءات JavaScript غير معالجة أثناء السيناريوهات التي اجتازت فحص المتصفح. الاتصال الخارجي معطّل عمدًا في هذا الفحص، لذلك لا يشمل توافر الخدمات الخارجية.

## تحديث تقليل التمرير — 22 سبتمبر 2026

- مقدمة نافذة الخدمة أصبحت صفًا صغيرًا بأيقونة 40px، واسم ووصف واضحين. شروط الخدمة باقية كاملة، وكل عنصر يحتفظ بارتفاع محتواه لمنع تداخل النصوص.
- قسم بيانات الحساب / الشحن أصبح أصغر، مع حقول ارتفاعها 40px وخط 16px، وملاحظات إضافية قابلة للفتح. لا تتداخل أيقونة الحقل مع البيانات المكتوبة.
- فودافون كاش وإنستاباي وتلدا تبدأ تفاصيلها مقفولة في الخدمة والسلة. الضغط يفتح وسيلة واحدة فقط، والضغط عليها ثانية يقفلها. اختيار وسيلة الدفع محفوظ أثناء إغلاق تفاصيلها؛ النسخ والروابط ما زالا متاحين.
- تقليل مسافات الأقسام، وحجم تذييل الطلب على الهاتف، وحشو كروت الدفع. شبكة الخدمات والباقات المصغرة مستمرة.
- بداية مظلمة لأي زيارة بدون تفضيل محفوظ، مع استمرار حفظ الاختيار اليدوي. تحسين نصوص ومؤشرات الوضع الفاتح فقط دون تغيير لوحة ألوان الوضع المظلم.
- فحص Chromium على 320 و390 و1440 بكسل في الوضعين: عدم تداخل المقدمة، إدخال بيانات الشحن، فتح الملاحظات، تبديل طرق الدفع وإغلاقها، وعدم التمدد الأفقي. بداية مظلمة اختُبرت بمتصفح بإعداد نظام فاتح؛ إعادة التحميل حافظت على اختيار المستخدم.
- أُضيف رقم إصدار لروابط ملفات التنسيق والسكربتات المعدّلة لتحديث نسخة المتصفح بعد الرفع. ارفع محتويات الحزمة كاملة بما فيها مجلد assets، ثم أعد تحميل الصفحة.
