# تحسين تجربة الهاتف — 24 سبتمبر 2026

- إصلاح أقواس غير مغلقة في `assets/design-panel.css` كانت تجعل قواعد الهاتف متداخلة داخل قاعدة أخرى.
- شبكة مقروءة افتراضيًا بخدمتين في الصف. يحتفظ المالك باختيار العرض العمودي أو الشبكة؛ العرض الضيق يحد الأعمدة إلى اثنين والتابلت إلى ثلاثة مهما كانت الإعدادات القديمة.
- الاسم العربي يظهر كاملًا، والسعر وزر الطلب أوضح، وأزرار اللمس الرئيسية لا تقل عن 44 بكسل. حقول الهاتف 16 بكسل لتفادي تكبير الإدخال التلقائي في iOS.
- هيدر بارتفاع 114 بكسل بدل نحو 211 في اختبار 390 بكسل، بحث واضح، وحالة نشطة للتنقل السفلي، مع مراعاة حواف شاشة الهاتف.
- نافذة الطلب تملأ ارتفاع الشاشة المتاح، باقات في عمودين، ملخص سعر لا يكسر العملة، ومساحة أكبر للقراءة. في الارتفاعات القصيرة يمكن تمرير المحتوى والأزرار معًا.
- ترتيب وسائل الدفع يدعم الالتفاف على الهاتف، والتفاصيل تظهر بعد الاختيارات. التنقل والمساعدة الخلفية تختفي أثناء فتح الطلب أو السلة.
- تحسين حقول السلة، محرر الخدمة، وإعدادات التصميم على الهاتف، وإصلاح تعارض الخلفية مع الوضع الفاتح.
- حذف 3,487 إعلان CSS فارغ أو فارغ مع `!important` كان المتصفح يتجاهله. لم تتغير أسعار الخدمات أو أرقام الدفع أو بيانات العملاء.

## التحقق

```text
node validate_all.js
node test_catalog.js
node --test tests/storefront.test.cjs tests/fulfillment.test.cjs tests/reviews.test.cjs tests/publish.test.mjs tests/styles.test.cjs
node tests/browser.cjs
node tests/mobile-browser.cjs
powershell -File test_audit.ps1
```

اختبار الهاتف يستخدم Chrome بمحاكاة اللمس ومقاسات 320×568، 360×640، 390×844، 430×932، 768×1024، 844×390. يشمل أيضًا ارتفاع 360 بكسل لمحاكاة المساحة الضيقة عند فتح لوحة المفاتيح، رحلة الطلب والسلة، الإعدادات القديمة المتطرفة، الوضع الفاتح، رفض دخول الزائر للإدارة، ومحرر الخدمة ولوحة التصميم. لا يُرسل الاختبار طلبات أو رسائل أو تغييرات للسحابة.

هذه محاكاة متصفح وليست اختبارًا على جهاز iPhone فعلي. تسجيل دخول المالك والنشر السحابي وإرسال واتساب الحقيقي تحتاج اختبارًا بحساب المالك؛ اختبارات الرحلة تستخدم بدائل محلية لها.

المعاينة المحلية عبر `node tools/preview.cjs` على `http://127.0.0.1:4173/` تعطل اتصال Firebase في الاستجابات المحلية.
