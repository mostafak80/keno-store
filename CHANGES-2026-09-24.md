# تحديث المتجر — 24 سبتمبر 2026

التعديلات موجودة في ملفات المشروع المحلي، ولم تُرفع إلى GitHub أو Vercel أو Firebase. الدومين المضبوط: https://keno-store.vercel.app/، والمستودع الذي حدده المالك: https://github.com/mostafak80/keno-store.

## ما تم تنفيذه

| الطلب | التنفيذ |
|---|---|
| التحكم في بيانات الشحن | محرر داخل «تعديل الخدمة» للعنوان والشرح والخصوصية، وإضافة وحذف الخانات وتحديد نوعها وإلزاميتها والمثال والتعليمات. |
| متطلبات مستقلة لكل خدمة | `fulfillment` محفوظ لكل خدمة ويصمد أمام الحفظ والاستيراد والتصدير والنشر؛ لا يوجد استنتاج كلمة المرور من تصنيف الألعاب. |
| وصف أمان صادق | إزالة وعد عدم طلب كلمة مرور مطلقًا؛ شرح مستقل لكل خدمة. الخدمات التي تتطلب دخولًا توضّح الاتفاق على التنفيذ في المحادثة. كلمات المرور لا تُجمع أو تُخزن في الموقع أو روابط واتساب. |
| بيانات كل عنصر في السلة | كل إضافة سطر مستقل، حتى للباقة نفسها، مع بياناته وملخصه وتفاصيله في رسالة واتساب وسجل الطلب والإدارة. زيادة الكمية في السطر تكون لنفس الحساب. |
| التقييمات | إزالة التقييمات المصطنعة ووسم التوثيق والتواريخ النسبية الثابتة. العميل يختار طلبه من الجهاز نفسه أو يستخدم رابط تقييم يصدره المتجر، بعد تسليم الطلب. تعليق واحد لكل خدمة في الطلب. |
| تنظيم CSS | `style.css` نقطة دخول بترتيب تحميل واضح؛ توزيع التنسيقات على 8 ملفات في `assets/styles` وإزالة 1298 تصريحًا مغطى بتصريحات لاحقة لنفس المحدد والسياق، وإفراد تنسيقات المكونات الجديدة في `assets/journey.css`. هذا تقليل فعلي للتكرار، وليس ادعاء إزالة كل تعارض محتمل في التنسيقات القديمة. |
| الدومين والسحابة | canonical وOpen Graph وJSON-LD وrobots وsitemap على العنوان الفعلي. إعداد الدومين وتصدير ملفات SEO من الإدارة. إصلاح عرض بيانات الشحن في تفاصيل الطلب، وقراءة الطلبات السحابية، والإبلاغ عن فشل تحديثها. |
| 3 خطوات | اختيار الباقة ← بيانات الشحن ← مراجعة وواتساب. قسم خطوة واحد ظاهر، مع التالي والرجوع وحفظ الاختيارات أثناء التنقل. السلة 3 خطوات أيضًا. |
| أسماء مفهومة | أسماء عربية للخدمات مع وصف وظيفتها والأسعار الفعلية. نظام صورة الخدمة الموجود مستمر، مع بديل واضح عند عدم وجود صورة. |
| اسمع الخطوة | زر ثابت الموضع في الطلب والسلة؛ تسجيلات عامة للخطوات، وتسجيلات مخصصة لكل خدمة، تشغيل بالضغط فقط. |
| المساعدة بالفويس | زر ثابت على الصفحة، وزر داخل نافذة الطلب والسلة يفتح واتساب؛ تسجيل الصوت يتم داخل واتساب. |
| شرح مصور | صورة مستقلة لكل خانة مع وصف نصي، تحت «أجيبه منين؟»، ومراجعة الرقم قبل الإرسال بدون الادعاء بالتحقق من الحساب. |
| الباقات الكثيرة والميزانية | حتى 8 مختارات يحددها المالك، وإلا أرخص 4 باقات متاحة، مع «كل الباقات» وفلتر ميزانية حسب السعر الفعلي. المختار خارج الميزانية يوقف التالي حتى تعديله. |
| إدارة المحتوى | تحرير نصوص الواجهة والأسئلة، الشعار واللون الأساسي، إظهار الأقسام، ضمانات المتجر، بيانات الاتصال والدفع، الخدمات والصور والباقات، التسجيلات والدومين. تغيير بنية الصفحة البرمجية أو إعدادات حسابات الاستضافة يظل عملًا برمجيًا/تشغيليًا. |

## خطوات استخدام الإدارة

1. افتح `/#admin` وسجل دخولك بحساب المالك المعتمد.
2. من الخدمات اضغط «تعديل»، ثم قسم «بيانات الحساب / الشحن لهذه الخدمة». حدّد خانات التنفيذ الحقيقي لهذه الخدمة واحفظها.
3. لكل خانة يمكنك وضع رابط HTTPS لصورة الشرح، أو رفع PNG/JPEG/WebP حتى 100 كيلوبايت. اكتب وصفًا يشرح المثال في الصورة. الملفات الأكبر تستخدم رابط HTTPS. صور الشرح لا تُقص حتى لا تضيع الأسهم أو الأرقام.
4. في نفس القسم ارفع تسجيلًا مصريًا لكل خطوة أو أدخل رابطه. يدعم MP3/WAV/OGG/WebM؛ حد الملف المضمن 100 كيلوبايت. التسجيلات العامة في الإعدادات تستخدم عندما تكون تسجيلات الخدمة فارغة.
5. حدّد الباقات المختارة. لإدراج باقة أضفتها للتو في المختارات، احفظ الخدمة وافتحها مرة أخرى. الأسعار تظل مأخوذة من الباقات الأصلية.
6. من الإعدادات افتح «محتوى الواجهة، أقسام الموقع، الدومين والتسجيلات». يمكنك تعديل النصوص، الشعار واللون والأقسام والتسجيلات العامة وعبارات الثقة.
7. احفظ المسودة، وافتح معاينتها، ثم استخدم نشر التغييرات بعد التأكد من بيانات حساب المالك والسحابة.

التسجيلات وصور الشرح الفعلية ليست مرفقة؛ يلزم رفع ملفات مناسبة من المالك. الزر الصوتي يظل معطّلًا عند عدم إضافة تسجيل. أمثلة للتسجيل باللهجة المصرية:

- «اختار الباقة اللي محتاجها، وبعدها اضغط التالي.»
- «اكتب بيانات الحساب المطلوب شحنه. لو مش عارف تجيب الرقم، اضغط أجيبه منين.»
- «راجع رقم الحساب والباقة والسعر، علّم إنك راجعت البيانات، وبعدها افتح واتساب.»

الخدمات ذات طريقة التنفيذ غير المحددة في المصدر تطلب وصف الطلب أو تتفق على التنفيذ في واتساب؛ راجعها من محرر الخدمة. ببجي الكورية تستخدم ربط الحساب طبقًا للملاحظات الأصلية؛ لا تطلب ID. حساب ChatGPT المشترك لا يطلب بيانات حساب العميل الشخصي.

## تشغيل التعليقات

- نشر `firestore.rules` الجديد شرط لتشغيلها في الإنتاج. لا يكفي نشر ملفات الواجهة وحدها.
- يحفظ الطلب رمز تقييم عشوائيًا خاصًا ومعرّفات الخدمات. لا يسمح إرسال التعليق إلا لطلب حالته `delivered` وخدمة موجودة داخله ورمز مطابق.
- تحفظ التعليقات الأصلية في `reviewSubmissions` الخاصة بالمشرفين. تنشر الإدارة نسخة عامة في `reviews` لا تحتوي رمز الطلب أو بيانات حساب العميل.
- لا يمكن تغيير رأي العميل أو النجوم أثناء النشر؛ يمكن نشر التعليق أو إخفاؤه. التاريخ من وقت إنشاء الخادم الأصلي.
- للطلب القديم بلا معرّفات خدمات: راجع تفاصيله، واختر الخدمة المشتراة في قسم تعليقات الإدارة، ثم أنشئ رابط التقييم. لا تُصدر الرابط إلا لصاحب الطلب.
- لا تظهر شارة «مشترٍ موثق». لا تُعاد صناعة تعليقات قديمة غير مثبتة.

بناء قواعد التقييم يعتمد على [التحقق من المستندات داخل قواعد Firestore](https://firebase.google.com/docs/firestore/security/rules-conditions) وعلى [وقت الخادم](https://firebase.google.com/docs/reference/rules/rules.Timestamp).

## الاختبارات والحدود

- 45 اختبار Node ناجح: الكتالوج والأسعار، متطلبات الخدمة والوسائط، استقلال بيانات السلة، طلبات النشر وتوثيق الرمز، وعزل أسرار التقييم وصون نصه وتاريخه والتعامل مع فشل السحابة.
- اختبار Chrome محلي ناجح: الخطوات، الإدخال الإلزامي، الميزانية، الرجوع، حسابان لنفس الباقة، رسائل الطلب والسجلات، تعديل الإدارة، حفظ المسودة واستعادتها، المعاينة، البحث، وسائل الدفع، الوضعان، الحركة المخفضة والتخزين المحظور، وفشل النشر بدون ادعاء النجاح.
- فحص مقاسات 320 و390 و768 و1440 دون تمرير أفقي للصفحة أو نافذة الطلب. صور المراجعة في `qa/`.
- فحص JavaScript وملفات الربط وحوار الإغلاق ناجح.
- اختبارات المتصفح تستخدم سحابة بديلة محلية؛ لم تُنشئ طلبات أو تعليقات أو منشورات في حساب الإنتاج. قواعد Firestore لم تُختبر على المحاكي أو تُنشر في الإنتاج؛ يلزم اختبارها ضمن حساب المالك قبل التشغيل.
- فتح الموقع المنشور نجح. محاولة دخول Google من متصفح الجلسة رجعت `auth/network-request-failed`. لا يكفي ذلك للجزم بأن إعدادات Firebase خاطئة، ولا يثبت نجاح تسجيل الدخول.

أوامر الفحص:

```powershell
node validate_all.js
node test_catalog.js
node --test tests/storefront.test.cjs tests/fulfillment.test.cjs tests/reviews.test.cjs tests/publish.test.mjs
node tests/browser.cjs
./test_audit.ps1
node tools/preview.cjs
```

اختبار المتصفح يحتاج Playwright وChrome، ويمكن تحديد `CODEX_PRIMARY_RUNTIME_NODE_MODULES` و`CHROMIUM_EXECUTABLE_PATH` لبيئتك. المعاينة على `http://127.0.0.1:4173` تعطل سحابة الإنتاج ولا تصلح لاختبار تسجيل دخول المالك.

## ما يلزم قبل النشر الفعلي

1. راجع المسودة والملفات، ثم ارفع ملفات المشروع إلى المستودع المذكور ليعيد Vercel النشر. لم يتم رفع أي شيء من هذه الجلسة.
2. انشر قواعد Firestore الجديدة في مشروع `keno-store` بحساب المالك، واختبر رفض تعليق برمز خاطئ أو طلب غير مكتمل، وقبول تعليق واحد لخدمة مشتراة بعد التسليم، وعدم إتاحة مستند التعليق الخاص للعامة.
3. تأكد من السماح بالدومين `keno-store.vercel.app` في Firebase Authentication، وتفعيل Google، وإعداد متغيرات خادم النشر الموثقة في README (`GITHUB_TOKEN`, `GITHUB_REPO`, `ADMIN_EMAILS`). لا تضع مفاتيح الخادم في الكتالوج.
4. إذا كان هناك كتالوج منشور سابقًا في `settings/catalog`، فهو يتقدم على الملف المرفق. خذ نسخته الاحتياطية وادمج إعدادات التنفيذ والأسماء معه من الإدارة قبل النشر كي لا تفقد أسعارًا أحدث.
5. نفّذ طلب اختبار وعلّمه كطلب اختبار، ثم اختبر حفظه، تسليمه والتعليق عليه. اختبر نشر تعديل بسيط من حساب المالك ورؤيته من متصفح آخر.

عند تغيير الدومين مستقبلًا، احفظه في الإدارة ونزّل ملفات SEO وارفعها أيضًا؛ تحديث الكتالوج في السحابة لا يعيد كتابة ملفات الاستضافة الثابتة وحده.
